// Supabase ক্লায়েন্ট (একবারই লিখো)
const supabaseUrl = 'https://ooihnyqssqubgshvakyg.supabase.co';
const supabaseKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9vaWhueXFzc3F1YmdzaHZha3lnIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA5MDIwNTIsImV4cCI6MjA4NjQ3ODA1Mn0.yZ0istNTaImVOF7M4aeO_OfEBx-xQKVvywVNJLavMD0';
const supabase = supabase.createClient(supabaseUrl, supabaseKey);

// লগইন চেক + UI আপডেট
let currentUser = null;

async function checkAuth() {
  const { data: { session } } = await supabase.auth.getSession();
  currentUser = session?.user || null;

  const userInfo = document.getElementById('user-info');
  const logoutBtn = document.getElementById('logout-btn');
  const authSection = document.getElementById('auth-section');

  if (currentUser) {
    userInfo.innerText = `Welcome, ${currentUser.email}`;
    logoutBtn.style.display = 'inline-block';
    if (authSection) authSection.style.display = 'none';
    loadHistory(); // Supabase থেকে লোড করো
  } else {
    userInfo.innerText = '';
    logoutBtn.style.display = 'none';
    if (authSection) authSection.style.display = 'block';
  }
}

// Sign Up
async function signUp() {
  const email = document.getElementById('email').value.trim();
  const password = document.getElementById('password').value.trim();
  const msg = document.getElementById('auth-message');

  if (!email || !password) {
    msg.innerText = 'Please enter email and password';
    return;
  }

  const { error } = await supabase.auth.signUp({ email, password });
  msg.innerText = error ? 'Error: ' + error.message : 'Account created! Check email to confirm.';
}

// Login
async function login() {
  const email = document.getElementById('email').value.trim();
  const password = document.getElementById('password').value.trim();
  const msg = document.getElementById('auth-message');

  if (!email || !password) {
    msg.innerText = 'Please enter email and password';
    return;
  }

  const { data, error } = await supabase.auth.signInWithPassword({ email, password });
  if (error) {
    msg.innerText = 'Login failed: ' + error.message;
  } else {
    msg.innerText = 'Welcome, ' + data.user.email;
    checkAuth();
  }
}

// Log Out
async function logout() {
  await supabase.auth.signOut();
  checkAuth();
}

// QR সেভ করার ফাংশন (Supabase-এ)
async function saveQRToDatabase(qrContent, qrType = 'text') {
  if (!currentUser) {
    alert('Please log in to save your QR history.');
    return;
  }

  const { error } = await supabase
    .from('user_qr_history')
    .insert({
      user_id: currentUser.id,
      qr_content: qrContent,
      qr_type: qrType
    });

  if (error) {
    console.error('Save error:', error.message);
    alert('Failed to save: ' + error.message);
  } else {
    console.log('QR saved to Supabase');
  }
}

// হিস্ট্রি লোড করা (Supabase থেকে)
async function loadHistory() {
  if (!currentUser) {
    // লগইন না থাকলে পুরনো localStorage হিস্ট্রি দেখাতে পারো
    let qrHistory = JSON.parse(localStorage.getItem('qrHistory')) || [];
    renderHistory(qrHistory);
    return;
  }

  const { data, error } = await supabase
    .from('user_qr_history')
    .select('qr_content, created_at')
    .eq('user_id', currentUser.id)
    .order('created_at', { ascending: false });

  if (error) {
    console.error('Load error:', error.message);
    return;
  }

  renderHistory(data.map(item => ({
    text: item.qr_content,
    dataURL: '', // Supabase-এ image সেভ করলে এখানে URL যোগ করতে পারো
    created_at: item.created_at
  })));
}

// হিস্ট্রি দেখানো (render function)
function renderHistory(history) {
  const historyList = document.getElementById('history-list');
  historyList.innerHTML = '';
  history.forEach((item, index) => {
    const div = document.createElement('div');
    div.className = 'history-item';
    div.innerHTML = `
      <span>${item.text}</span>
      ${item.dataURL ? `<img src="${item.dataURL}" alt="QR">` : ''}
      <button onclick="downloadHistory(${index}, '${item.text}')">ডাউনলোড</button>
    `;
    historyList.appendChild(div);
  });
}

// তোমার generateQR ফাংশন (আপডেট করা)
function generateQR() {
  const text = document.getElementById('text-input').value.trim();
  if (!text) return alert('টেক্সট লিখো!');

  const qrPreview = document.getElementById('qr-preview');
  qrPreview.innerHTML = '';
  const qrcode = new QRCode(qrPreview, {
    text: text,
    width: 256,
    height: 256,
    colorDark: "#000000",
    colorLight: "#ffffff",
    correctLevel: QRCode.CorrectLevel.H
  });

  setTimeout(() => {
    const canvas = qrPreview.querySelector('canvas');
    const dataURL = canvas.toDataURL('image/png');

    // Supabase-এ সেভ করো
    saveQRToDatabase(text);

    document.getElementById('download-btn').style.display = 'block';
    document.getElementById('download-btn').setAttribute('data-url', dataURL);

    // হিস্ট্রি আপডেট করো
    loadHistory();
  }, 100);
}

// তোমার downloadQR, downloadHistory, clearHistory ফাংশন রাখো
// clearHistory-এর জন্য Supabase থেকে ডিলিট করার লজিক যোগ করতে পারো (optional)

function clearHistory() {
  if (confirm('নিশ্চিত?')) {
    if (currentUser) {
      // Supabase থেকে ডিলিট (optional)
      supabase.from('user_qr_history').delete().eq('user_id', currentUser.id);
    }
    localStorage.removeItem('qrHistory');
    loadHistory();
  }
}

// ইনিশিয়াল লোড
window.onload = function() {
  checkAuth();
  // প্রোফাইল লোড (যদি localStorage-এ থাকে)
  let profile = JSON.parse(localStorage.getItem('profile')) || { name: 'Md Riyad', pic: '' };
  saveAndDisplayProfile(profile.name);
};
