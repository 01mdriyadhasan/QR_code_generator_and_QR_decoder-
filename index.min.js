// Supabase Configuration
const supabaseUrl = 'https://ooihnyqssqubgshvakyg.supabase.co';
const supabaseKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im9vaWhueXFzc3F1YmdzaHZha3lnIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA5MDIwNTIsImV4cCI6MjA4NjQ3ODA1Mn0.yZ0istNTaIm';
const supabase = window.supabase.createClient(supabaseUrl, supabaseKey);

let currentUser = null;

// Toggle Mobile Menu
function toggleMenu() {
  const menu = document.getElementById('menu');
  if (menu) {
    menu.classList.toggle('active');
  }
}

// Check Authentication Status
async function checkAuth() {
  const { data: { session } } = await supabase.auth.getSession();
  currentUser = session?.user || null;

  const userInfo = document.getElementById('user-info');
  const logoutBtn = document.getElementById('logout-btn');
  const authSection = document.getElementById('auth-section');

  if (currentUser) {
    if (userInfo) userInfo.innerText = `Welcome, ${currentUser.email}`;
    if (logoutBtn) logoutBtn.style.display = 'inline-block';
    if (authSection) authSection.style.display = 'none';
    loadHistory();
  } else {
    if (userInfo) userInfo.innerText = '';
    if (logoutBtn) logoutBtn.style.display = 'none';
    if (authSection) authSection.style.display = 'block';
  }
}

// Sign Up
async function signUp() {
  const email = document.getElementById('email').value.trim();
  const password = document.getElementById('password').value.trim();
  const msg = document.getElementById('auth-message');

  if (!email || !password) {
    if (msg) msg.innerText = 'Please enter email and password';
    return;
  }

  const { error } = await supabase.auth.signUp({ email, password });
  if (msg) {
    msg.innerText = error ? 'Error: ' + error.message : 'Account created! Check email to confirm.';
  }
}

// Login
async function login() {
  const email = document.getElementById('email').value.trim();
  const password = document.getElementById('password').value.trim();
  const msg = document.getElementById('auth-message');

  if (!email || !password) {
    if (msg) msg.innerText = 'Please enter email and password';
    return;
  }

  const { data, error } = await supabase.auth.signInWithPassword({ email, password });
  if (error) {
    if (msg) msg.innerText = 'Login failed: ' + error.message;
  } else {
    if (msg) msg.innerText = 'Welcome, ' + data.user.email;
    checkAuth();
  }
}

// Log Out
async function logout() {
  await supabase.auth.signOut();
  checkAuth();
}

// Save QR to Database
async function saveQRToDatabase(qrContent, qrType = 'text') {
  if (!currentUser) {
    console.log('Not logged in - saving to localStorage');
    let history = JSON.parse(localStorage.getItem('qrHistory')) || [];
    history.unshift({ text: qrContent, timestamp: new Date().toLocaleString() });
    localStorage.setItem('qrHistory', JSON.stringify(history.slice(0, 20)));
    return;
  }

  const { error } = await supabase
    .from('user_qr_history')
    .insert({
      user_id: currentUser.id,
      qr_content: qrContent,
      qr_type: qrType
    });

  if (error) {
    console.error('Save error:', error.message);
  } else {
    console.log('QR saved successfully');
  }
}

// Load QR History
async function loadHistory() {
  if (!currentUser) {
    let qrHistory = JSON.parse(localStorage.getItem('qrHistory')) || [];
    renderHistory(qrHistory);
    return;
  }

  const { data, error } = await supabase
    .from('user_qr_history')
    .select('qr_content, created_at')
    .eq('user_id', currentUser.id)
    .order('created_at', { ascending: false });

  if (error) {
    console.error('Load error:', error.message);
    return;
  }

  renderHistory(data.map(item => ({
    text: item.qr_content,
    timestamp: new Date(item.created_at).toLocaleString()
  })));
}

// Render History
function renderHistory(history) {
  const historyList = document.getElementById('history-list');
  if (!historyList) return;
  
  historyList.innerHTML = '';
  if (history.length === 0) {
    historyList.innerHTML = '<p style="color: #999;">No QR codes generated yet</p>';
    return;
  }
  
  history.forEach((item, index) => {
    const div = document.createElement('div');
    div.className = 'history-item';
    div.innerHTML = `
      <span>${item.text}</span>
      <button onclick="deleteHistoryItem(${index})" style="background: #dc3545; padding: 5px 10px; font-size: 0.9em;">Delete</button>
    `;
    historyList.appendChild(div);
  });
}

// Delete History Item
function deleteHistoryItem(index) {
  let history = JSON.parse(localStorage.getItem('qrHistory')) || [];
  history.splice(index, 1);
  localStorage.setItem('qrHistory', JSON.stringify(history));
  loadHistory();
}

// Generate QR Code
function generateQR() {
  const textInput = document.getElementById('text-input');
  const text = textInput ? textInput.value.trim() : '';
  
  if (!text) {
    alert('Please enter text or URL!');
    return;
  }

  const qrPreview = document.getElementById('qr-preview');
  if (!qrPreview) return;
  
  qrPreview.innerHTML = '';
  
  try {
    const qrcode = new QRCode(qrPreview, {
      text: text,
      width: 256,
      height: 256,
      colorDark: "#000000",
      colorLight: "#ffffff",
      correctLevel: QRCode.CorrectLevel.H
    });

    setTimeout(() => {
      const canvas = qrPreview.querySelector('canvas');
      if (canvas) {
        const dataURL = canvas.toDataURL('image/png');
        saveQRToDatabase(text);

        const downloadBtn = document.getElementById('download-btn');
        if (downloadBtn) {
          downloadBtn.style.display = 'block';
          downloadBtn.setAttribute('data-url', dataURL);
        }

        loadHistory();
      }
    }, 100);
  } catch (error) {
    console.error('QR generation error:', error);
    alert('Error generating QR code');
  }
}

// Download QR Code
function downloadQR() {
  const downloadBtn = document.getElementById('download-btn');
  if (!downloadBtn) return;
  
  const dataURL = downloadBtn.getAttribute('data-url');
  if (!dataURL) return alert('Generate a QR code first!');

  const link = document.createElement('a');
  link.href = dataURL;
  link.download = 'qrcode_' + new Date().getTime() + '.png';
  link.click();
}

// Clear History
function clearHistory() {
  if (confirm('Are you sure you want to clear all QR history?')) {
    if (currentUser) {
      supabase.from('user_qr_history').delete().eq('user_id', currentUser.id);
    }
    localStorage.removeItem('qrHistory');
    loadHistory();
  }
}

// Initialize on Page Load
window.addEventListener('load', function() {
  checkAuth();
});